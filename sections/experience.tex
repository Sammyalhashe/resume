\section{Experience}
  \resumeSubHeadingListStart

    \resumeSubheading
      {Bloomberg}{New York, NY}
      {Senior Software Engineer}{Jan 2024 -- Present}
    \resumeSubSubheading
      {Software Engineer}{Sept 2020 -- Jan 2024}
      \resumeItemListStart
        % Scale is expressed as model count, not throughput: DMPA shows a busy broker
        % hosting ~103 models with only ~22 publishing, most under 4 msgs/s. Message
        % rate is the wrong axis for this system -- fan-out and per-model isolation are
        % the real story. A fleet-wide model or subscriber count would be stronger here.
        \resumeItem{Core developer on an internal \textbf{C++} pub/sub middleware streaming real-time market data to Bloomberg Terminal clients across IB, MSG, and GP; each broker hosts 100+ independently entitled and rate-limited publishing models.}
          \begin{itemize}[leftmargin=0.3in]
            \item\small{Added two-way metadata exchange so subscribers retune parameters in place, replacing a two-round-trip unsubscribe/resubscribe and a fragile convention of smuggling parameters through unrelated subscription fields.}
            \item\small{Collapsed the settings pipeline to a single JSON field, removing the lockstep database/service/client schema update every new setting required --- and a deploy-order race that dropped requests or crashed clients on decode.}
          \end{itemize}
          \vspace{-4pt}
        \resumeItem{Integrating company-wide routing and entitlement libraries into the framework, replacing per-team configuration with a unified request-routing and access-control layer; in alpha, rolling out team by team.}
        \resumeItem{Built and maintained a Cython binding layer exposing the C++ SDK to Python consumers; became one of Bloomberg's most widely used internal libraries, adopted by 50+ application teams for middleware integration.}
        \resumeItem{Extended the framework to web clients over HTTP --- JWT-secured JSON transport behind an nginx reverse proxy --- plus an OpenAPI specification and JavaScript client SDK standardizing web-client integration across Bloomberg.}
        \resumeItem{Wrote a \textbf{Python} bot that triages $\sim$25 false-positive alerts/week via Kafka and batched HTTP requests --- each previously $\sim$5 minutes of cross-referencing affected machines against open maintenance tickets --- reclaiming $\sim$2 engineer-hours/week.}
      \resumeItemListEnd

    \resumeSubheading
      {MThree (Contracted to Morgan Stanley)}{New York, NY}
      {Software Developer}{Aug 2019 -- Aug 2020}
      \resumeItemListStart
        \resumeItem{Engineered a Java/Spring Boot backend handling 500+ entitlement ticket requests weekly, interfacing with internal APIs across the entitlement system.}
      \resumeItemListEnd

  \resumeSubHeadingListEnd
